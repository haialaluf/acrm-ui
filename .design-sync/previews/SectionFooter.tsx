import { SectionFooter, Button } from "acrm-ui";

export const WithAction = () => (
  <div style={{ width: 420 }}>
    <SectionFooter>
      <Button className="primary">Save changes</Button>
    </SectionFooter>
  </div>
);
